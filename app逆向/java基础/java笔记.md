## java是编译型语言，需要经过编译才能运行

---

- 类的名字要首字母大写，并且要和文件名一致 文件名=类名.java
- 方法名字首字母小写且驼峰命名

          <br />

flag = true;等效于 boolean flag = true;
char d = 'a';等效于 char d = 97;
强制转换：int a = (int)3.14; // 3
++i 先自增，再赋值，反之先赋值
continue; // 跳出当前循环，继续下一次循环
break; // 跳出当前循环，结束循环
